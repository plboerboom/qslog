{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "ExerciseRecord",
  "description": "TO COME",
  "type": "object",
  "properties": {
    "datetime": {
      "type": "string",
      "format": "date-time"
    },
    "workout": {"$ref": "#/definitions/sequence"}
  },

  "required" : ["datetime", "workout"],

  "definitions": {
    "sequence" : {
      "type": "array",
      "items": {
        "anyOf": [
          {"$ref": "#/definitions/sequence"},
          {"$ref": "#/definitions/atomicExercise"}
          ]
      }
    },
    "atomicExercise": { "oneOf": [
      {"$ref": "#/definitions/deadlift"},
      {"$ref": "#/definitions/ringPullup"}
    ]
    },
    "baseExercise": {
      "type": "object",
      "properties": {
        "name": {"type": "string"}
      },
      "required": ["name"]
    },
    "deadlift": {
      "allOf": [
        {"$ref": "#/definitions/baseExercise"},
        {"properties":
          { "name": { "pattern": "Deadlift" } }
        }
          ]
    },
    "ringPullup": { "allOf": [
      {"$ref": "#/definitions/baseExercise"},
      {"properties": {
        "name": {
          "pattern": "Ring pullup"
        }
      }
      }
        ]
    }
  }
}
